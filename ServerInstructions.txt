in vscode open the terminal (or open a new one if needed by going to the top of the window > Terminal > new Terminal)
type npm start into the terminal
It will automatically open the webpage, but if not copy paste the local host command it returns in your browser.
when finished, go back to the terminal and press ctrl + c to close the server